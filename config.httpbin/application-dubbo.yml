service:
  version:
    httpbin: v1
dubbo:
  enabled: true
  scan:
    base-packages: io.flomesh.demo.simple.dubbo
  registry:
    address: zookeeper://zk.local:2181
grpc:
  server:
    port: -1 #disable grpc server